---
title: Internacionalização
description: Alterne as strings de UI de um app Amarra entre inglês e português com a env var LOCALE.
sidebar:
  order: 10
---

As strings de UI do Amarra vêm de um catálogo baseado em chaves em `pkg/cais/i18n`. Uma env var seleciona o locale, e os templates leem as strings através de um par de funções de template. Não há negociação de Accept-Language: o locale é fixo por processo.

## Escolher um locale

Defina `LOCALE` no ambiente (ou no `.env` local):

```bash
LOCALE=en   # default
LOCALE=pt   # Portuguese
```

`cais.Config.Locale` é lido de `LOCALE` e usa `en` como padrão quando não definido. O catálogo é passado ao renderer no boot — `cais.NewRenderer(fsys, catalog)` — e um catálogo nil recorre a `DefaultCatalog()`.

## Usar strings nos templates

Os templates chamam `{{ t "key" }}` para traduzir, e `{{ htmlLang }}` / `{{ ogLocale }}` para emitir os códigos atuais de idioma e locale:

```html
<html lang="{{ htmlLang }}">
  <body>
    <h1>{{ t "contact.title" }}</h1>
  </body>
</html>
```

- `htmlLang` retorna a tag de idioma HTML (`en`, `pt-BR`).
- `ogLocale` retorna o locale do Open Graph (`en_US`, `pt_BR`).

Páginas e partials recebem as mesmas funções de template, então `{{ t }}` funciona em ambas. Os handlers recebem o `*i18n.Catalog` diretamente para validação e strings de flash, por exemplo `h.catalog.T("contact.title")`. Uma chave ausente retorna a própria chave, o que torna as lacunas visíveis durante o desenvolvimento.

## API do catálogo

```go
// pkg/cais/i18n
const DefaultLocale = "en"

func NewCatalog(locale string) *Catalog
func DefaultCatalog() *Catalog

func (c *Catalog) T(key string, args ...any) string
func (c *Catalog) HTMLLang() string        // "en", "pt-BR"
func (c *Catalog) OGLocale() string        // "en_US", "pt_BR"
func (c *Catalog) Funcs() template.FuncMap // t, htmlLang, ogLocale
```

`T` aceita argumentos opcionais para substituição na string.

## Adicionar ou estender um locale

Os catálogos ficam em `pkg/cais/i18n`:

- `locales/en.go` — todas as chaves, em inglês (o padrão).
- `locales/pt.go` — traduções em português, usadas quando `LOCALE=pt`.

Adicione uma chave em `en.go` primeiro, depois traduza-a em `pt.go`. Para adicionar outro locale, adicione um arquivo `locales/<code>.go` com as mesmas chaves e selecione-o com `LOCALE=<code>`.

:::caution
A v1 é deliberadamente pequena: sem locale por usuário, sem Accept-Language, sem pluralização e sem catálogos externos em JSON/YAML. As strings ficam em mapas Go sob `pkg/cais/i18n/locales/`.
:::

## Relacionado

- [Helpers de template](/amarra-cais/pt-br/docs/reference/template-helpers/) — as funções `t`, `htmlLang` e `ogLocale`.
- [Configuração](/amarra-cais/pt-br/docs/reference/configuration/) — `LOCALE` e as outras variáveis de ambiente.
